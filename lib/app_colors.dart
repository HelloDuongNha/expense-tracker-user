import 'package:flutter/material.dart';

// Centralized color definitions for the entire app.
class AppColors {
  // Main screen background color.
  static const Color screenBackground = Color(0xFFF8FAFC);

  // Light surface color for dividers and subtle backgrounds.
  static const Color surfaceLight = Color(0xFFF1F5F9);

  // Light blue tinted background for highlighted containers.
  static const Color blueLight = Color(0xFFEFF6FF);

  // Very light indigo background for icon containers and avatars.
  static const Color indigoLight = Color(0xFFEEF2FF);

  // Light border color for cards, inputs, and dividers.
  static const Color borderLight = Color(0xFFE2E8F0);

  // Default border color for drag handles and unselected outlines.
  static const Color borderDefault = Color(0xFFCBD5E1);

  // Slate gray for secondary icons, hint text, and labels.
  static const Color slateGray = Color(0xFF94A3B8);

  // Secondary text color for descriptions and subtitles.
  static const Color textSecondary = Color(0xFF64748B);

  // Dark text color for app bar foregrounds and budget values.
  static const Color textDark = Color(0xFF1E293B);

  // Primary text color for titles and headings.
  static const Color textPrimary = Color(0xFF0F172A);

  // Primary blue used for buttons, links, and active states.
  static const Color primaryBlue = Color(0xFF2563EB);

  // Green color for active and success status indicators.
  static const Color successGreen = Color(0xFF22C55E);

  // Darker green for positive remaining budget values.
  static const Color successGreenDark = Color(0xFF16A34A);

  // Light green background for approved and joined badges.
  static const Color successGreenLight = Color(0xFFDCFCE7);

  // Dark green text color for success badge labels.
  static const Color successGreenText = Color(0xFF166534);

  // Amber color for on-hold and warning status indicators.
  static const Color warningAmber = Color(0xFFF59E0B);

  // Light amber background for pending status badges.
  static const Color warningAmberLight = Color(0xFFFEF3C7);

  // Red color for errors, danger actions, and closed status.
  static const Color errorRed = Color(0xFFEF4444);

  // Light red background for rejected status badges.
  static const Color errorRedLight = Color(0xFFFEE2E2);

  // Dark red text color for rejected badge labels.
  static const Color errorRedText = Color(0xFF991B1B);

  // Pink color for the favorite heart icon.
  static const Color favoritePink = Color(0xFFE91E63);
}

